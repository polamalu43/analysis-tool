import {
  FOOTER_TITLE,
} from '../../const';

const Footer = (): JSX.Element => {
  return (
    <>
      <div className='footer'>
        <h5>{ FOOTER_TITLE }</h5>
      </div>
    </>
  );
}

export default Footer;
